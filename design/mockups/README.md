Export each Figma frame as PNG (2x) into this folder, named exactly as the frame
name in `docs/FIGMA_DESIGN_NOTES.md` section 3 (e.g. `Review.png`,
`Insights.png`). These files are committed on purpose so Claude Code can view the
actual design instead of relying on a verbal description.
