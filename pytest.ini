[pytest]
# Works from the repo root or backend/ (see the root conftest.py). `.` exposes
# contracts/, stages/ and shared/; `backend` exposes the `app` package the same
# way uvicorn sees it.
testpaths = tests
pythonpath = . backend
